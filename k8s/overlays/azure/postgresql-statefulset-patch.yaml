apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
  namespace: exai # Important d'inclure le namespace pour que Kustomize cible correctement
spec: {} # On laisse un spec vide, car le patch ne modifie plus le volumeClaimTemplates.
# Supprimer toute la section volumeClaimTemplates d'ici. 